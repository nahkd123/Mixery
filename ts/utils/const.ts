/** 
 * The timing type. There're 3 different types:
 * 
 * - ``note``: The entire envelope is 1 note long
 * - ``seconds``: Use seconds
 * - ``bpm``: Use BPM (beats per minutes)
 */
type TimingType = "note" | "seconds" | "bpm";